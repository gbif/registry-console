import React from 'react';
import { FormattedMessage } from 'react-intl';

const NotFound = () => (
  <div>
    <h1><FormattedMessage id="notFound" defaultMessage="Page not found"/></h1>
  </div>
);

export default NotFound;